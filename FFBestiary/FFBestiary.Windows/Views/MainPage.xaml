<controls:PageBase
    x:Class="FFBestiary.Views.MainPage"
    prism:ViewModelLocator.AutoWireViewModel="True"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FFBestiary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="using:Microsoft.Practices.Prism.Mvvm"
    xmlns:controls="using:FFBestiary.Controls"
    xmlns:vm="using:FFBestiary.ViewModels"
    xmlns:design="using:FFBestiary.DesignTime"
    d:DataContext="{d:DesignInstance vm:MainPageViewModel}"
    mc:Ignorable="d">
    
    <d:Page.DataContext>
        <design:MainPageViewModel />
    </d:Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="{StaticResource HubHeaderThemeFontSize}"/>
            <Button Content="Get Enemy" Command="{Binding GetEnemyCommand}">
                
            </Button>
        </StackPanel>
    </Grid>
</controls:PageBase>
